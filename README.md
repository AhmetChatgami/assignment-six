1) What is the difference between var, let, and const?
Ans: var is old method variable name;
        let variable value can be change but const value cannot be change.

2) What is the difference between map(), forEach(), and filter()? 
Ans: map() is a non-mutating method. It creates a new array with the results.

filter() creates new array with elements that pass the test, and forEach() do modify external variable or can update the DOM.

3) What are arrow functions in ES6?

Ans: Arrow function is most popular JavaScript function which is alternative to traditional function. If there are many property the we can declare arrow function, Exm: const name=(num, num2) => num+num2

4) How does destructuring assignment work in ES6?
Ans: Array destructuring relies on the position of the elements where use square brackets ([]) to define the variables.

 5) Explain template literals in ES6. How are they different from string concatenation?
Ans: Template literals are strings delimited by backtick characters (` `) instead of single quotes ('') or double quotes (""). (Pardon me. I didn't understand the Qst. So, I answered it from GPT)